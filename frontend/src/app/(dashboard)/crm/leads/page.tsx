import { LeadsView } from '@/components/dashboard/crm/leads/LeadsView';

export const metadata = {
  title: 'Leads | Krishna Events ERP',
  description: 'Manage sales inquiries and leads',
};

export default function LeadsPage() {
  return <LeadsView />;
}
