import { LeadDetailView } from '@/components/dashboard/crm/leads/LeadDetailView';

export const metadata = {
  title: 'Lead Details | Krishna Events ERP',
  description: 'View inquiry details and timeline',
};

export default function LeadDetailPage() {
  return <LeadDetailView />;
}
