import { SiteVisitDetailView } from '@/components/dashboard/crm/site-visits/SiteVisitDetailView';

export const metadata = {
  title: 'Site Visit Details | Krishna Events ERP',
  description: 'View inspection report and venue details',
};

export default function SiteVisitDetailPage() {
  return <SiteVisitDetailView />;
}
