import { CalendarEventDetail } from '@/components/dashboard/calendar/CalendarEventDetail';

export const metadata = {
  title: 'Event Details | Krishna Events ERP',
  description: 'View event details',
};

export default function EventDetailPage() {
  return <CalendarEventDetail />;
}
