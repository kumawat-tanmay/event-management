import React from 'react';
import { DispatchDetailView } from '@/components/dashboard/logistics/dispatches/DispatchDetailView';

export const metadata = {
  title: 'Dispatch Details | Krishna Events ERP',
};

export default function DispatchDetailPage() {
  return <DispatchDetailView />;
}
